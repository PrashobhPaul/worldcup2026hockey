node_modules/
dist/
.DS_Store
*.log

__pycache__/
*.pyc

# Generated by `npx cap add android` at build time — derived entirely from
# capacitor.config.json and dist/, so a checked-in copy only rots. Anchored to
# the root: unanchored, this also swallowed resources/android/, which holds the
# launcher icons the build copies INTO the generated project.
/android/
